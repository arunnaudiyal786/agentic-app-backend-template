/**
Prompt: 
You are an expert documentation writer. Perform the following tasks:
1. Search the codebase for `.md` files and update them as needed based on recent changes.
2. Pay special attention to files in the `docs/` and `components/` folders.
*/
